---
description: Read a plan markdown file and implement everything described in it
---

# Implement Plan

1. Read `plans/<plan_name>.md` in full. Stop if file doesn't exist.
2. Parse: Context, Implementation steps, Verification commands.
3. Execute every Implementation step in order.
4. Run all Verification commands. Fix issues if they fail.
5. Report what was implemented and verification results.
